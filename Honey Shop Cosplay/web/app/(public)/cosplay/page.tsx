import type { Metadata } from 'next'; import { ProductCard } from '../../../components/ProductCard'; import { getProducts } from '../../../lib/api';
export const metadata: Metadata={title:'Kho đồ cosplay',description:'Xem các set cosplay, giá test/fes/shoot và tình trạng đang có tại Honey Shop.'};
export default async function CatalogPage(){const products=await getProducts();return <main className="min-h-screen bg-[#fff6dc] py-14"><div className="shell"><p className="font-extrabold uppercase tracking-[.22em] text-[#f544a8]">Cosplay closet</p><h1 className="display mt-2 max-w-4xl text-6xl font-extrabold leading-[.9] md:text-8xl">Chọn một nhân vật. Viết một câu chuyện.</h1><div className="mt-8 flex flex-wrap gap-2">{['Tất cả','Anime','Game','Fantasy','Có sẵn'].map((x,i)=><button key={x} className={`rounded-full border-2 border-[#24150e] px-4 py-2 text-sm font-bold ${i===0?'bg-[#ffe75c]':'bg-white'}`}>{x}</button>)}</div>{products.length?<div className="mt-12 grid gap-8 md:grid-cols-2 lg:grid-cols-3">{products.map((p,i)=><ProductCard key={p.id} product={p} index={i}/>)}</div>:<div className="mt-12 border-2 border-dashed border-[#24150e] p-12 text-center"><h2 className="display text-3xl font-bold">Kho đồ đang nghỉ một chút</h2><p className="mt-2">Hãy kiểm tra Worker API hoặc seed D1 local.</p></div>}</div></main>}
