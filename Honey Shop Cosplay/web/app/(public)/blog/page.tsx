import type { Metadata } from 'next'; import Link from 'next/link'; import { getPosts } from '../../../lib/api';
export const metadata:Metadata={title:'Chuyện Honey'};
export default async function BlogPage(){const posts=await getPosts();return <main className="min-h-screen bg-[#ffe75c] py-16"><div className="shell"><p className="font-extrabold uppercase tracking-[.22em] text-[#7957d5]">Journal</p><h1 className="display mt-2 text-7xl font-extrabold">Chuyện ở Honey</h1><div className="mt-12 grid gap-6 md:grid-cols-2">{posts.map((p,i)=><Link href={`/blog/${p.slug}`} key={p.id} className={`sticker min-h-72 p-8 transition hover:-translate-y-2 ${i%2?'bg-[#55ddea]':'bg-white'}`}><span className="text-xs font-extrabold uppercase tracking-widest">{p.type==='GUIDE'?'Hướng dẫn':'Bài viết'}</span><h2 className="display mt-8 text-4xl font-extrabold leading-tight">{p.title}</h2><p className="mt-4 leading-7 text-[#624b40]">{p.excerpt}</p><b className="mt-7 block">Đọc tiếp →</b></Link>)}</div></div></main>}
