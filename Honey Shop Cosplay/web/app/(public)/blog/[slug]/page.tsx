import Link from 'next/link'; import { notFound } from 'next/navigation'; import { getPost } from '../../../../lib/api';
export default async function ArticlePage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const post=await getPost(slug);if(!post)notFound();return <main className="bg-[#fff6dc] py-14"><article className="shell max-w-3xl"><Link href="/blog" className="font-bold">← Về blog</Link><div className="sticker mt-7 bg-white p-7 md:p-12"><span className="font-extrabold uppercase tracking-widest text-[#f544a8]">{post.type==='GUIDE'?'Hướng dẫn':'Chuyện Honey'}</span><h1 className="display mt-4 text-5xl font-extrabold leading-tight md:text-7xl">{post.title}</h1><p className="mt-7 text-xl leading-9 text-[#624b40]">{post.excerpt}</p><div className="mt-10 border-t-2 border-dashed border-[#24150e]/30 pt-8 text-base leading-8 whitespace-pre-wrap">{post.content}</div></div></article></main>}
