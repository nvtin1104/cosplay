import Link from 'next/link'; import { getPosts } from '../../../lib/api';
export default async function GuidePage(){const guides=(await getPosts()).filter(p=>p.type==='GUIDE');return <main className="min-h-screen bg-[#55ddea] py-16"><div className="shell"><p className="font-extrabold uppercase tracking-[.22em]">How to rent</p><h1 className="display mt-2 max-w-4xl text-6xl font-extrabold leading-[.9] md:text-8xl">Thuê đồ vui, trả đồ cũng thật nhẹ nhàng.</h1><div className="mt-12 grid gap-6 md:grid-cols-3">{[['01','Đo size','Đo ngực, eo, mông và chiều cao trước khi chọn biến thể.'],['02','Kiểm tra lịch','Shop xác nhận ngày thuê và số lượng trước khi nhận cọc.'],['03','Giữ đồ đẹp','Không tự giặt, không chỉnh sửa và báo ngay nếu có sự cố.']].map((x,i)=><article key={x[0]} className={`sticker p-7 ${i===1?'bg-[#ffe75c]':'bg-white'}`}><span className="display text-6xl font-extrabold text-[#f544a8]">{x[0]}</span><h2 className="display mt-4 text-3xl font-bold">{x[1]}</h2><p className="mt-3 leading-7">{x[2]}</p></article>)}</div>{guides.length>0&&<div className="mt-14"><h2 className="display text-4xl font-extrabold">Bài hướng dẫn chi tiết</h2><div className="mt-6 grid gap-4 md:grid-cols-2">{guides.map(g=><Link className="sticker bg-white p-6 font-extrabold" href={`/blog/${g.slug}`} key={g.id}>{g.title} →</Link>)}</div></div>}</div></main>}
